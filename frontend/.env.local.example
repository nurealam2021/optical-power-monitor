# Optional. Leave unset/blank in production when Nginx serves frontend and API
# from the same host. For local development the UI automatically uses :8000.
# NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
